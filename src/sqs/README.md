# src/sqs

Placeholder for optional SQS-based orchestration/decoupling (e.g., an S3
event notifying a queue to trigger an EMR processing step).

Not required for Phase 1 core big data processing (S3 -> EMR/PySpark ->
Redshift). Kept as an example/optional folder only — see
docs/PROJECT_CONTEXT.md for the note on why SQS is not a core Phase 1
service.

No code yet.
